# Local variables file — contains secrets, never commit
terraform.tfvars

# Terraform working directories
.terraform/
.terraform.lock.hcl

# State files — using remote S3 backend, but guard against local state
*.tfstate
*.tfstate.backup
*.tfstate.d/

# Plan output files
*.tfplan
